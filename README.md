# batch-file-renamer
A configurable Windows Batch script to bulk rename files recursively, supporting lowercase conversion, word removal, single-digit padding, space replacement, and collision prevention.
